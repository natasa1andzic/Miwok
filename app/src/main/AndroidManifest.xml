<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.activities">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.android.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:parentActivityName=".MainActivity"
            android:name="com.example.android.activities.NumbersActivity"
            android:label="@string/category_numbers" />
        <activity
            android:parentActivityName=".MainActivity"
            android:name="com.example.android.activities.FamilyActivity"
            android:label="@string/category_family" />
        <activity
            android:parentActivityName=".MainActivity"
            android:name="com.example.android.activities.ColorsActivity"
            android:label="@string/category_colors" />
        <activity
            android:parentActivityName=".MainActivity"
            android:name="com.example.android.activities.PhrasesActivity"
            android:label="@string/category_phrases" />
    </application>

</manifest>
